let count = 0;

const counterDisplay = document.querySelector("#display");
// assign the '+' button to a const named 'counterInc' here

counterInc.addEventListener("click", () => {
  // update count 
  updateDisplay();
});

 // add an event listener for the '-' button here
 //  don't forget to fetch the button element above!

function updateDisplay() {
  // update display
}
